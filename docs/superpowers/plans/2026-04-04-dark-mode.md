# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add dark mode to the entire Booking0Trip system using `next-themes` and Tailwind `dark:` variants.

**Architecture:** Install `next-themes`, enable `darkMode: 'class'` in Tailwind, wrap the root layout in `ThemeProvider`, create a `ThemeToggle` component, and add `dark:` Tailwind classes to all 23 files.

**Tech Stack:** Next.js 14, Tailwind CSS 3.4, next-themes, TypeScript

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `tailwind.config.ts` | Modify | Add `darkMode: 'class'` |
| `src/app/layout.tsx` | Modify | Wrap app in `ThemeProvider` |
| `src/app/globals.css` | Modify | Add `dark:` variants to all component classes |
| `src/components/ThemeToggle.tsx` | Create | Sun/moon toggle button |
| `src/components/Header.tsx` | Modify | Dark styles + add ThemeToggle |
| `src/components/MobileNav.tsx` | Modify | Dark styles |
| `src/components/LanguageToggle.tsx` | Modify | Dark styles |
| `src/components/LoadingSpinner.tsx` | Modify | Dark styles |
| `src/components/Toast.tsx` | Modify | Dark styles |
| `src/app/login/page.tsx` | Modify | Dark styles + add ThemeToggle |
| `src/app/signup/page.tsx` | Modify | Dark styles + add ThemeToggle |
| `src/app/(authenticated)/admin/page.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/trips/page.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/trips/[id]/page.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/trips/[id]/OverviewTab.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/trips/[id]/BusesTab.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/trips/[id]/RoomsTab.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/trips/[id]/UnbookedTab.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/users/page.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/logs/page.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/admin/reports/page.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/trips/page.tsx` | Modify | Dark styles |
| `src/app/(authenticated)/trips/[tripId]/buses/page.tsx` | Modify | Dark styles |

---

## Color Reference

| Pattern | Light | Dark |
|---|---|---|
| Body bg | `bg-slate-50` | `dark:bg-gray-950` |
| Card bg | `bg-white` | `dark:bg-gray-900` |
| Primary text | `text-slate-800` | `dark:text-gray-100` |
| Secondary text | `text-slate-500`, `text-slate-600` | `dark:text-gray-400` |
| Muted text | `text-slate-400` | `dark:text-gray-500` |
| Faint text | `text-slate-300` | `dark:text-gray-600` |
| Border | `border-slate-100`, `border-slate-200` | `dark:border-gray-800` |
| Header/nav bg | `bg-white/95` | `dark:bg-gray-900/95` |
| Input bg | `bg-white` | `dark:bg-gray-800` |
| Input border | `border-slate-200` | `dark:border-gray-700` |
| Placeholder | `placeholder:text-slate-300` | `dark:placeholder:text-gray-500` |
| Secondary btn bg | `bg-slate-100` | `dark:bg-gray-800` |
| Secondary btn text | `text-slate-700` | `dark:text-gray-200` |
| Hover slate-50 | `hover:bg-slate-50` | `dark:hover:bg-gray-800` |
| Hover slate-100 | `hover:bg-slate-100` | `dark:hover:bg-gray-700` |
| Stat bg slate | `bg-slate-50` | `dark:bg-gray-800` |
| Stat bg blue | `bg-blue-50` | `dark:bg-blue-950/30` |
| Stat bg red | `bg-red-50` | `dark:bg-red-950/30` |
| Stat bg purple | `bg-purple-50` | `dark:bg-purple-950/30` |
| Stat bg amber | `bg-amber-50` | `dark:bg-amber-950/30` |
| Stat bg emerald | `bg-emerald-50` | `dark:bg-emerald-950/30` |
| Stat text blue | `text-blue-700` | `dark:text-blue-400` |
| Stat text red | `text-red-600` | `dark:text-red-400` |
| Stat text purple | `text-purple-700` | `dark:text-purple-400` |
| Stat text slate | `text-slate-700` | `dark:text-gray-300` |
| Badge blue | `bg-blue-50 text-blue-700` | `dark:bg-blue-950/30 dark:text-blue-400` |
| Badge green | `bg-emerald-50 text-emerald-700` | `dark:bg-emerald-950/30 dark:text-emerald-400` |
| Badge red | `bg-red-50 text-red-700` | `dark:bg-red-950/30 dark:text-red-400` |
| Badge amber | `bg-amber-50 text-amber-700` | `dark:bg-amber-950/30 dark:text-amber-400` |
| Error bg | `bg-red-50` | `dark:bg-red-950/50` |
| Error text | `text-red-600` | `dark:text-red-400` |
| Active nav | `bg-blue-50 text-blue-700` | `dark:bg-blue-950/50 dark:text-blue-400` |
| Inactive nav | `text-slate-400` | `dark:text-gray-500` |
| Nav border | `border-slate-200` | `dark:border-gray-800` |
| Login gradient | `from-blue-50 via-white to-slate-50` | `dark:from-gray-950 dark:via-gray-900 dark:to-gray-950` |
| Gender inactive | `border-slate-200 bg-white text-slate-600` | `dark:border-gray-700 dark:bg-gray-800 dark:text-gray-300` |
| Gender active | `border-blue-500 bg-blue-50 text-blue-700` | `dark:border-blue-500 dark:bg-blue-950/50 dark:text-blue-400` |
| Checkbox border | `border-slate-300` | `dark:border-gray-600` |
| Focus ring | `focus:ring-blue-500/40` | `dark:focus:ring-blue-400/40` |
| Focus border | `focus:border-blue-500` | `dark:focus:border-blue-400` |
| Scrollbar | `bg-slate-200` | `dark:bg-gray-700` |
| Progress bg | `bg-slate-100` | `dark:bg-gray-800` |
| Tab active border | `border-blue-600` | `dark:border-blue-400` |
| Tab active text | `text-blue-700` | `dark:text-blue-400` |
| Tab inactive text | `text-slate-400` | `dark:text-gray-500` |
| Toggle closed bg | `bg-slate-100 text-slate-500 border-slate-200` | `dark:bg-gray-800 dark:text-gray-400 dark:border-gray-700` |
| Blue-600/700 text accents | `text-blue-600`, `text-blue-700` | Keep same or use `dark:text-blue-400` for dark surfaces |
| Pink badge | `bg-pink-50 text-pink-600` | `dark:bg-pink-950/30 dark:text-pink-400` |
| Selected ring | `ring-blue-500` | `dark:ring-blue-400` |
| Section title | `text-slate-800` | `dark:text-gray-100` |

---

### Task 1: Install next-themes and configure Tailwind

**Files:**
- Modify: `tailwind.config.ts`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Install next-themes**

```bash
npm install next-themes
```

- [ ] **Step 2: Update tailwind.config.ts**

Add `darkMode: 'class'` at the top level of the config object.

- [ ] **Step 3: Update src/app/layout.tsx**

Wrap `I18nProvider` with `ThemeProvider` from `next-themes`:

```tsx
import { ThemeProvider } from "next-themes";

// In the return, wrap existing providers:
<ThemeProvider attribute="class" defaultTheme="light">
  <I18nProvider>
    <ToastProvider>
      {children}
    </ToastProvider>
  </I18nProvider>
</ThemeProvider>
```

- [ ] **Step 4: Verify build**

```bash
npm run build
```

---

### Task 2: Create ThemeToggle component

**Files:**
- Create: `src/components/ThemeToggle.tsx`

- [ ] **Step 1: Create the component**

```tsx
"use client";

import { useTheme } from "next-themes";
import { useEffect, useState } from "react";

export default function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    setMounted(true);
  }, []);

  if (!mounted) {
    return <div className="w-9 h-9" />;
  }

  return (
    <button
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
      className="p-2 rounded-lg text-slate-400 hover:bg-slate-100 dark:hover:bg-gray-800 dark:text-gray-500 active:scale-95 transition-all duration-150"
      title={theme === "dark" ? "Switch to light mode" : "Switch to dark mode"}
    >
      {theme === "dark" ? (
        <svg xmlns="http://www.w3.org/2000/svg" className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
        </svg>
      ) : (
        <svg xmlns="http://www.w3.org/2000/svg" className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
        </svg>
      )}
    </button>
  );
}
```

---

### Task 3: Update globals.css with dark variants

**Files:**
- Modify: `src/app/globals.css`

- [ ] **Step 1: Add dark variants to all component classes in globals.css**

Every `@apply` in the component layer gets corresponding `dark:` variants:

- `body`: add `dark:bg-gray-950 dark:text-gray-100`
- `.btn-primary`: add `dark:shadow-blue-400/10`
- `.btn-secondary`: add `dark:bg-gray-800 dark:text-gray-200 dark:hover:bg-gray-700`
- `.btn-danger`: add `dark:bg-red-950/50 dark:text-red-400 dark:hover:bg-red-950`
- `.input-field`: add `dark:bg-gray-800 dark:border-gray-700 dark:text-gray-100 dark:placeholder:text-gray-500 dark:focus:ring-blue-400/40 dark:focus:border-blue-400`
- `.label-text`: add `dark:text-gray-400`
- `.card`: add `dark:bg-gray-900 dark:border-gray-800`
- `.badge-blue`: add `dark:bg-blue-950/30 dark:text-blue-400`
- `.badge-green`: add `dark:bg-emerald-950/30 dark:text-emerald-400`
- `.badge-red`: add `dark:bg-red-950/30 dark:text-red-400`
- `.badge-amber`: add `dark:bg-amber-950/30 dark:text-amber-400`
- `.section-title`: add `dark:text-gray-100`
- `.progress-bar`: add `dark:bg-gray-800`
- Scrollbar: add `dark:bg-gray-700`

---

### Task 4: Update Header.tsx with dark styles + ThemeToggle

**Files:**
- Modify: `src/components/Header.tsx`

- [ ] **Step 1: Add ThemeToggle import and dark classes**

Add `import ThemeToggle from "./ThemeToggle"` and place `<ThemeToggle />` next to `<LanguageToggle />` in both mobile and desktop sections. Add dark variants to:
- Header bg: `dark:bg-gray-900/95`
- Border: `dark:border-gray-800`
- Title text: `dark:text-blue-400`
- Nav active: `dark:bg-blue-950/50 dark:text-blue-400`
- Nav inactive: `dark:text-gray-500 dark:hover:bg-gray-800 dark:hover:text-gray-300`
- Welcome text: `dark:text-gray-500`
- Logout mobile: `dark:hover:bg-gray-800`
- Logout desktop: `dark:hover:bg-red-950/50`

---

### Task 5: Update MobileNav.tsx with dark styles

**Files:**
- Modify: `src/components/MobileNav.tsx`

- [ ] **Step 1: Add dark variants**

- Nav bg: `dark:bg-gray-900/95`
- Border: `dark:border-gray-800`
- Active tab: `dark:text-blue-400`
- Inactive tab: `dark:text-gray-500`

---

### Task 6: Update shared components (LanguageToggle, LoadingSpinner, Toast)

**Files:**
- Modify: `src/components/LanguageToggle.tsx`
- Modify: `src/components/LoadingSpinner.tsx`
- Modify: `src/components/Toast.tsx`

- [ ] **Step 1: Add dark variants to each component**

LanguageToggle:
- Button bg: `dark:hover:bg-gray-800`
- Active text: `dark:text-blue-400`
- Inactive text: `dark:text-gray-400`

LoadingSpinner:
- Spinner border: `dark:border-gray-700`
- Spinning border: keep `border-blue-600` or use `dark:border-blue-400`
- Text: `dark:text-gray-400`

Toast:
- Toast bg success: `dark:bg-emerald-950 dark:text-emerald-400 dark:border-emerald-800`
- Toast bg error: `dark:bg-red-950 dark:text-red-400 dark:border-red-800`
- Toast bg info: `dark:bg-blue-950 dark:text-blue-400 dark:border-blue-800`

---

### Task 7: Update auth pages (login, signup)

**Files:**
- Modify: `src/app/login/page.tsx`
- Modify: `src/app/signup/page.tsx`

- [ ] **Step 1: Add ThemeToggle import and place next to LanguageToggle**

Both pages have `<LanguageToggle />` in the top-right — add `<ThemeToggle />` next to it.

- [ ] **Step 2: Add dark variants to login page**

- Background gradient: `dark:from-gray-950 dark:via-gray-900 dark:to-gray-950`
- Icon bg: `dark:bg-blue-950/30`
- Icon text: `dark:text-blue-400`
- Title text: `dark:text-gray-100`
- Checkbox label: `dark:text-gray-400`
- Checkbox border: `dark:border-gray-600`
- Error bg: `dark:bg-red-950/50`
- Error text: `dark:text-red-400`
- Footer text: `dark:text-gray-400`
- Link: `dark:text-blue-400 dark:hover:text-blue-300`

- [ ] **Step 3: Add dark variants to signup page**

Same pattern as login, plus:
- Gender inactive buttons: `dark:border-gray-700 dark:bg-gray-800 dark:text-gray-300`
- Gender active buttons: `dark:border-blue-500 dark:bg-blue-950/50 dark:text-blue-400`

---

### Task 8: Update admin pages

**Files:**
- Modify: `src/app/(authenticated)/admin/page.tsx`
- Modify: `src/app/(authenticated)/admin/trips/page.tsx`

- [ ] **Step 1: Add dark variants to admin dashboard**

- Stat boxes: apply per-color dark variants from the color reference table
- Card hover: already handled by globals.css `.card-hover`
- Text classes: `dark:text-gray-100` for headings, `dark:text-gray-500` for muted

- [ ] **Step 2: Add dark variants to trips management page**

- Form card: already handled by globals.css `.card`
- Toggle button open: `dark:bg-blue-950/30 dark:text-blue-400 dark:border-blue-800`
- Toggle button closed: `dark:bg-gray-800 dark:text-gray-400 dark:border-gray-700`
- Manage button: `dark:bg-blue-950/30 dark:text-blue-400 dark:hover:bg-blue-950/50`
- Edit button: `dark:bg-gray-800 dark:text-gray-400 dark:hover:bg-gray-700`
- Delete button: `dark:bg-red-950/30 dark:text-red-400 dark:hover:bg-red-950/50`
- Open badge: `dark:bg-emerald-950/30 dark:text-emerald-400`
- Closed badge: `dark:bg-gray-800 dark:text-gray-400`

---

### Task 9: Update trip detail page and tabs

**Files:**
- Modify: `src/app/(authenticated)/admin/trips/[id]/page.tsx`
- Modify: `src/app/(authenticated)/admin/trips/[id]/OverviewTab.tsx`
- Modify: `src/app/(authenticated)/admin/trips/[id]/BusesTab.tsx`
- Modify: `src/app/(authenticated)/admin/trips/[id]/RoomsTab.tsx`
- Modify: `src/app/(authenticated)/admin/trips/[id]/UnbookedTab.tsx`

- [ ] **Step 1: Update trip detail page**

- Back button: `dark:text-blue-400 dark:hover:text-blue-300`
- Tab border: `dark:border-gray-800`
- Tab active: `dark:border-blue-400 dark:text-blue-400`
- Tab inactive: `dark:text-gray-500 dark:hover:text-gray-300`
- Date text: `dark:text-gray-500`
- Error text: `dark:text-gray-500`

- [ ] **Step 2: Update OverviewTab**

- Stat boxes: apply per-color dark variants
- Area headings: `dark:text-gray-100`
- Badge colors: apply dark badge variants
- Empty state text: `dark:text-gray-500`, `dark:text-gray-600`

- [ ] **Step 3: Update BusesTab**

- Heading: `dark:text-gray-100`
- Bus name: `dark:text-gray-100`
- Status badges: apply dark variants per color
- Edit/delete buttons: apply dark variants
- Leader name text: `dark:text-gray-500`
- Empty state: `dark:text-gray-500`, `dark:text-gray-600`

- [ ] **Step 4: Update RoomsTab**

- Headings: `dark:text-gray-100`
- Gender tab active: keep `bg-blue-600 text-white`
- Gender tab inactive: `dark:bg-gray-800 dark:text-gray-400 dark:hover:bg-gray-700`
- Filter active: `dark:bg-blue-950/30 dark:text-blue-400`
- Filter inactive: `dark:bg-gray-800 dark:text-gray-400 dark:hover:bg-gray-700`
- Occupant name: `dark:text-gray-400`
- Selected ring: `dark:ring-blue-400`
- Pink badge: `dark:bg-pink-950/30 dark:text-pink-400`
- Blue badge: `dark:bg-blue-950/30 dark:text-blue-400`
- Remove button: `dark:text-red-400 dark:hover:text-red-300`

- [ ] **Step 5: Update UnbookedTab**

- Headings: `dark:text-gray-100`
- Gender buttons: same pattern as signup
- Register form card: handled by globals.css
- Passenger list items: `dark:text-gray-300`
- Search input: handled by globals.css `.input-field`
- Empty state: `dark:text-gray-500`

---

### Task 10: Update remaining admin pages

**Files:**
- Modify: `src/app/(authenticated)/admin/users/page.tsx`
- Modify: `src/app/(authenticated)/admin/logs/page.tsx`
- Modify: `src/app/(authenticated)/admin/reports/page.tsx`

- [ ] **Step 1: Add dark variants to each page**

Apply the standard color mapping:
- Headings: `dark:text-gray-100`
- Card content: handled by globals.css `.card`
- Secondary text: `dark:text-gray-400` / `dark:text-gray-500`
- Action buttons: apply dark variants per color
- Table rows if any: `dark:border-gray-800`
- Badges: apply dark badge variants

---

### Task 11: Update patient-facing pages

**Files:**
- Modify: `src/app/(authenticated)/trips/page.tsx`
- Modify: `src/app/(authenticated)/trips/[tripId]/buses/page.tsx`

- [ ] **Step 1: Add dark variants to trips page and buses page**

- Headings: `dark:text-gray-100`
- Card content: handled by globals.css
- Status badges: apply dark badge variants
- Empty state text: `dark:text-gray-500`
- Action buttons: apply dark variants

---

### Task 12: Verify and build

- [ ] **Step 1: Run the build**

```bash
npm run build
```

- [ ] **Step 2: Run linting**

```bash
npm run lint
```

- [ ] **Step 3: Run tests**

```bash
npm test
```

All must pass with no errors.
